<script setup lang="ts">
import type { PendingAction } from '@/types'
defineProps<{action:PendingAction}>(); defineEmits<{confirm:[];cancel:[]}>()
</script>
<template><div class="overlay" @click.self="$emit('cancel')"><section class="sheet"><div class="handle"></div><div class="warn">⚠️ Xác nhận trước khi lưu</div><h3>{{action.previewTitle}}</h3><div class="preview"><div v-for="line in action.previewLines" :key="line">{{line}}</div></div><p>AI chưa thay đổi dữ liệu. Chỉ lưu sau khi bạn xác nhận.</p><div class="buttons"><button class="cancel" @click="$emit('cancel')">Hủy</button><button class="confirm" @click="$emit('confirm')">Xác nhận</button></div></section></div></template>
<style scoped>.overlay{position:fixed;inset:0;background:rgba(15,23,42,.38);display:flex;align-items:flex-end;z-index:50}.sheet{background:white;width:100%;max-width:560px;margin:0 auto;border-radius:24px 24px 0 0;padding:12px 18px calc(20px + env(safe-area-inset-bottom));box-shadow:0 -10px 30px rgba(15,23,42,.18)}.handle{width:42px;height:5px;border-radius:5px;background:#cbd5e1;margin:0 auto 12px}.warn{font-size:.82rem;font-weight:800;color:#b45309}.preview{background:#f8fafc;border-radius:14px;padding:12px;display:grid;gap:7px}.sheet p{font-size:.82rem;color:#64748b}.buttons{display:grid;grid-template-columns:1fr 1.4fr;gap:10px}.buttons button{border:0;border-radius:14px;padding:13px;font-weight:800}.cancel{background:#f1f5f9}.confirm{background:#1d4ed8;color:white}</style>
